import React from 'react';
import Header from './Header';
import Menu from './Menu.js';

const TopBar = () => {
    return (
        <div>
            <Header />
            <Menu />
        </div>
    );
}

export default TopBar;